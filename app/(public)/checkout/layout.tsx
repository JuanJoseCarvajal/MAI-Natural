import type { Metadata } from "next";
export const metadata: Metadata = { title: "Tu pedido | MAI Natural", robots: { index: false, follow: false }, alternates: { canonical: null } };
export default function CheckoutLayout({ children }: { children: React.ReactNode }) { return children; }
